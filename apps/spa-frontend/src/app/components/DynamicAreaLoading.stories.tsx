import { Meta, StoryObj } from '@storybook/react';
import { DynamicAreaLoading } from './DynamicAreaLoading';

export default {
  title: 'Components/DynamicAreaLoading',
  component: DynamicAreaLoading,
} as Meta;

export const Default: StoryObj<typeof DynamicAreaLoading> = {
  args: {},
};
